import PricingPage from '@/views/PricingPage';
import Head from 'next/head';

const Home = () => (
  <>
    <Head>
      <title>Pricing page</title>
    </Head>
    <PricingPage />
  </>
);

export default Home;
